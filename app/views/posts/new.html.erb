<% provide(:title, "New post | Mini-Reddit") %>
<div class="center jumbotron white-text">
  <h1>Create a new post</h1>
</div>

<div class="col-md-6 col-md-offset-3 white-text">
  <%= form_for(@post, url: mini_reddit_new_path) do |f| %>
    <%= render 'shared/post_error_messages' %>

    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.label :content %>
    <%= f.text_field :content, placeholder: "Place the link in this field..." %>

    <%= f.submit "Create post", class: "btn btn-primary" %>
  <% end %>
</div>
